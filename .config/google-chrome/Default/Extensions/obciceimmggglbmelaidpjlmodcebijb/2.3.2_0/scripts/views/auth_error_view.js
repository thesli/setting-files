// Generated by CoffeeScript 1.6.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  BH.Views.AuthErrorView = (function(_super) {

    __extends(AuthErrorView, _super);

    function AuthErrorView() {
      return AuthErrorView.__super__.constructor.apply(this, arguments);
    }

    AuthErrorView.include(BH.Modules.I18n);

    AuthErrorView.prototype.className = 'auth_error_view';

    AuthErrorView.prototype.template = BH.Templates['auth_error'];

    AuthErrorView.prototype.events = {
      'click .cancel': 'cancelClicked',
      'click .login': 'loginClicked'
    };

    AuthErrorView.prototype.initialize = function() {
      this.chromeAPI = chrome;
      this.tracker = this.options.tracker;
      return this.attachGeneralEvents();
    };

    AuthErrorView.prototype.render = function() {
      this.$el.html(this.renderTemplate(this.getI18nValues()));
      return this;
    };

    AuthErrorView.prototype.cancelClicked = function(ev) {
      ev.preventDefault();
      this.close();
      return user.logout();
    };

    AuthErrorView.prototype.loginClicked = function(ev) {
      var userProcessor;
      ev.preventDefault();
      this.close();
      user.logout();
      userProcessor = new BH.Lib.UserProcessor();
      return userProcessor.start();
    };

    AuthErrorView.prototype.getI18nValues = function() {
      return this.t(['login_again_button', 'prompt_cancel_button', 'auth_error_title', 'auth_error_description']);
    };

    return AuthErrorView;

  })(BH.Views.ModalView);

}).call(this);
